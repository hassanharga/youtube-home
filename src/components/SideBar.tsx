import { FC } from 'react';

const SideBar: FC = () => {
  return <aside className="sticky top-0 overflow-y-auto scrollbar-hidden pb-4 flex flex-col ml-1">Side bar</aside>;
};

export default SideBar;
